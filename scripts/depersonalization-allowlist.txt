# Depersonalization allowlist — consulted by check_depersonalization() in validate-plugin.sh
# Format: <repo-relative-path>:<matched-text>
# One entry per line; a line starting with # is a comment. Justification follows on
# the next comment line. Untracked dev artifacts never enter this file.

# servers/tests/test_filesystem.py:SECRET=hunter2\n
# Test fixture .env content asserting the sensitive-file denylist blocks access.
# "hunter2" is a well-known fake password used in test fixtures, not a real secret.
servers/tests/test_filesystem.py:SECRET=hunter2\n
